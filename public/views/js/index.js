const button = document.querySlector('button [type ="submit"]');
const input = document.querySelecetor('input [name="user-input']");

const userContent = [];
button.addEventListener('click',()=> {
const username = input.value;
user.Content.push(
for insert in userContent 

documento.querySelector('div [class = "item4"]").textContent = username